jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repo
      - uses: actions/checkout@v2
        name: Checkout code

      # Set up the Python environment
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      # Run the snake gif generation logic (your existing steps)
      - name: Generate snake gif
        run: |
          # Your logic to generate the snake gif
          
      # Push the generated file using GitHub token for authentication
      - name: Push gif to repository
        run: |
          git config --global user.email "meghanakotaru25@gmail.com"
          git config --global user.name "MeghanaKotharu25"
          git add github-contribution-grid-snake.svg
          git commit -m "Add contribution snake gif"
          git push https://x-access-token=${{ secrets.GH_TOKEN }}@github.com/MeghanaKotharu25/MeghanaKotharu25.git output
